---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "OIDC SPIFFE"
metadata:
  keywords:
    - "oidc"
    - "spiffe"
    - "spire"
    - "jwt-svid"
    - "workload-identity"
    - "security"
  categories:
    - "security"
  status: "experimental"
  config:
    - "quarkus.oidc-spiffe."
